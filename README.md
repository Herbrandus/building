# Building generator

This project is a visual random building generator that combines random shapes with different randomly chosen architectural ornaments to arrive at an ecclectic mix of styles to make every generated building unique.

## Dependencies

This project makes use of typescript and sass. To work on it, first use `npm install` and run `npm start` to serve the files with Gulp.